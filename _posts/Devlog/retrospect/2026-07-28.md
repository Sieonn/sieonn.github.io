# 📝 개발 회고 - 2026-07-28 (화)

## 🎯 오늘 한 일

- GridCell 헤더 아이콘 갱신 로직 정비 — 생성자의 유효성 체크를 isValidHeaderIconDef로 통일, updateIcon에서 크기 반영 누락 수정 (GridCell.js)
- refactor/grid 브랜치를 develop에 merge — 리베이스 중 발생한 문법 오류(dangling else-if) 및 토큰 참조 불일치 사전 수정 (a8321a4f7d)
- 리스트뷰 날짜 계산을 실제 점유 달력 날짜 기준으로 통일 — getTotalDaySpan 버그 수정 및 getListViewEventDays를 computeListViewEventDays로 추출해 공유 (CalendarEventFormat.ts, a64776db)
- 프레임 최초 저장 시 ID 치환 통합 — selectedFrameId/expandedFrameId/contentCollapsedFrameIds를 remapFrameIdReferences로 한곳으로 정리, 회귀 테스트 추가 (PageStore.tsx, 1acd28bc)
- more-popover 날짜 계산 버그 2건 수정 — date-only 문자열을 UTC로 파싱해 TZ에 따라 하루 더 계산되던 문제, start_full_datetime/end_full_datetime으로 변경 (CalendarMorePopover.tsx, 7be9347c, cdc25786f)
- 캘린더 TZ 회귀 테스트 재작성 — Asia/Seoul 양수 UTC offset을 전제하던 테스트를 실행 환경 TZ에 비의존적인 결정적 테스트로 재작성 (6315f560)

**커밋 요약**: 6개 주요 커밋, 다중 파일 변경 (캘린더/프레임/그리드 컴포넌트)

## 💡 배운 점 / 인사이트

- date-only 문자열과 full datetime의 타임존 파싱 경로가 다르면 계산 결과가 TZ에 따라 달라질 수 있다는 점 — 동일 로직을 다른 표현으로 사용하면 회귀가 생기기 쉬움
- 테스트 작성 시 테스트 코드의 전제(예: UTC offset의 부호)가 특정 실행 환경에 종속되면 CI/로컬 환경 차이로 플레이크 테스트가 될 수 있다 — 결정적인 테스트는 환경 변수에 좌우되지 않도록 설계해야 함
- 프레임 상태(접힘/선택/펼침)를 여러 곳에서 갱신하면 일관성 문제가 발생하기 쉽다 — 상태 치환 로직을 한곳으로 모으면 회귀 가능성을 줄일 수 있음

## 🧱 어려웠던 점 / 막힌 부분

- more-popover 버그의 근본 원인(date-only vs full datetime 파싱 차이)을 파악하는데 시간이 소요됨 — 타임존 로직이 여러 함수에 산재되어 있었음
- TZ 회귀 테스트가 로컬에서는 통과했지만 UTC 환경 CI에서 실패했고, 재작성 과정에서 여러 TZ 환경을 동시에 검증해야 했음 (해결됨)
- 프레임 ID 치환이 saveFrameInfo와 saveFramesBatch 두 경로에서 각각 다르게 구현되어 있어서 누락을 찾는데 시간 소요 (remapFrameIdReferences로 통합)

## 📌 내일 할 일

- [ ] 추가 타임존 엣지케이스 검증: DST 경계, 분단위 UTC 오프셋, GraphManagerBase의 exclusive-end 보정 자체
- [ ] 이번 라운드 테스트 53개(9개 파일) 커버리지 최종 점검
- [ ] 배치저장 다중 프레임 순서, contentCollapsedFrameIds 중복 등 엣지케이스 확인

## 📊 한 줄 요약

> 어제는 리뷰 피드백을 받은 칼럼 계산 로직과 타임존 관련 버그들을 차근차근 고쳐가는 하루였다. 테스트 환경 의존성 문제도 함께 해결.

---
*작성: 23:45*
