# 📝 개발 회고 - 2026-07-13 (월)

## 🎯 오늘 한 일

- **새 페이지 생성 시 그리드 dim 레이아웃 버그 수정** — EditGrid에서 가로 스크롤바 높이 미반영 문제를 근본 해결
  - 1차: BaseGrid에 getter 포워딩, GridManager에 observable 추가, 수동 sync 호출 → 놓친 경로(addNewColumn)로 재발
  - 2차: addNewColumn에 sync 호출 추가 → 동작 확인
  - 최종: encke-grid의 GridCallbackType에 `onScrollBarStateChanged` 콜백 신설, BaseGridCore.js resize() 끝에서 호출하도록 통합 → 흩어있던 9곳의 수동 호출 제거, 모든 레이아웃 변경 경로에서 자동 동기화

- **footer/스크롤바 경계 버그 검토** — 7컬럼 경계에서 footer 겹침 현상
  - calcFooterY 순서, sticky 분기 clamp 등 여러 가설 검토
  - 사용자가 스크린샷/devtools로 확인한 결과 "기존부터 있던 경계 케이스, 기능적 문제 없음" 판단 → 미해결 보류
  - 진행 중 추가한 임시 디버그 코드는 사용자가 직접 되돌림

- **스테이징된 변경사항 /code-review 리뷰 및 수정** — 2건의 버그 발견 및 개선
  - **ColumnMenu.tsx**: FOREIGN_KEY/LOOKR 컬럼의 "연결된 그리드로 이동" 아이콘이 sort 상태의 active/inactive 클래스 복붙 오용 → refGrid 존재 여부 기준으로 수정
  - **GraphCalendar.scss**: 주간/일간 뷰의 중복 스타일 블록을 `timegrid-view-common` 믹스인으로 통합, sass 컴파일 동일성 검증

## 💡 배운 점 / 인사이트

- **상태 동기화의 구조적 취약점**: encke-grid(canvas 엔진)와 React/MobX 레이어 사이에서 스크롤바 상태 같은 정보가 노출되지 않아, 개별 지점마다 수동 호출을 흩뿌려야 했던 설계 문제. 이번에 콜백 패턴으로 훅을 단일화했지만, 더 근본적으로는 엔진과 React 스토어 사이의 데이터 흐름을 재설계할 여지가 있음.

- **디버깅의 증거 우선주의**: footer 버그에서 가설만으로 여러 번 수정 시도했지만, 실제로는 사용자의 스크린샷과 콘솔 로그 증거가 훨씬 빠르고 확실했음. 예측 코딩보다 실제 데이터 확보가 더 중요.

- **코드 리뷰의 실제 가치**: /code-review 워크플로우에서 ColumnMenu의 className 복붙 오용, GraphCalendar의 중복 스타일 같은 버그들이 스테이징 단계에서 발견됨. 자동화된 리뷰 도구보다는 맥락 있는 검토가 더 효과적.

## 🧱 어려웠던 점 / 막힌 부분

- **1차/2차 수정 반복**: addNewColumn 경로를 처음에 놓쳐서 가호해결로 보였지만 재발. 컬럼 생성 UI의 모든 진입점(헤더 우클릭 메뉴, +버튼 등)을 완전히 파악하지 못함. 다음부터는 컬럼 추가 관련 전체 코드 맵을 먼저 그려야 할 것 같음.

- **footer 버그의 미해결**: 정확한 근본 원인을 특정하지 못한 채로 "경계 케이스"로 보류. 화면 배치/sticky 동작의 세부 로직이 복잡해서, 스크린샷으로는 확인 어렵던 부분이 있음.

## 📌 내일 할 일

- [ ] dim 버그 변경사항(onScrollBarStateChanged 콜백) 브랜치에서 검증, 이전에 남겨놓은 9곳 수동 호출이 정말 제거되었는지 확인
- [ ] footer 버그 → 기능적 문제 없다면 백로그에 옮기거나, 정확한 재현 조건을 명세화할지 검토
- [ ] GraphCalendar.scss 통합 후 다른 뷰(월간, 년간)와의 스타일 일관성 재검증

## 📊 한 줄 요약

> 구조적 문제(상태 동기화)를 콜백 패턴으로 해결하면서 동시에, 실제 증거를 바탕으로 디버깅하는 것의 중요성을 배우는 하루였다.

---
*작성: 17:04*
